# GPU-accelerated version (RECOMMENDED for production)
# Install with: pip install -r requirements_gpu.txt

# ONNX Runtime with GPU support (CUDA required)
onnxruntime-gpu>=1.17.0

# Core dependencies
opencv-python>=4.5.0
numpy>=1.21.0

# Scientific computing
scipy>=1.7.0

# Visualization
matplotlib>=3.4.0
Pillow>=8.0.0

# Utilities
tqdm>=4.62.0
shapely>=1.8.0

# FastReID (optional, for REID features)
# Install from source: git clone https://github.com/JDAI-CV/fast-reid.git && cd fast-reid && pip install -e .
# Requires: torch, torchvision, fvcore, iopath, yacs, and other dependencies

# GPU Requirements:
# - NVIDIA GPU with CUDA support
# - CUDA 11.8 or 12.x installed
# - cuDNN installed
# - NVIDIA drivers up to date
# 
# Verification:
# python -c "import onnxruntime as ort; print(ort.get_available_providers())"
# Should show: ['CUDAExecutionProvider', 'CPUExecutionProvider']

